name: Build and runtime

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  runtime:
    uses: bertie-mc/bertie-ci/.github/workflows/neoforge-mod.yml@v2
    with:
      sides: client
